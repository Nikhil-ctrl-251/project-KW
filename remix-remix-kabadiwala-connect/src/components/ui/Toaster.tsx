import React from 'react';

// For simplicity in the prototype, we can use a basic alert or a simple global state,
// but let's just make it a placeholder. We'll use standard window.alert or basic UI for errors
// since we want to focus on the core flow.

export function Toaster() {
  return null;
}
